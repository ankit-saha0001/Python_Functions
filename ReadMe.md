This is the assignment 3 of the Gen AI Course of TuteDude. Using Python I have understood the concept of :-

1. User -defined function
2. Recursive function
3. Lambda function
4. map() funtion
5. filter() function
6. calling function in continuous loop

Requirements for running my code:-

1. Python 3.x
2. Jupyter Notebook
3. Anaconda
4. Visual Studio Code

Task Mapping:-
| Task | Task Name | Task Description
|-----| ---------| ---------------|
| Task 1 | Price after discount | We used function to discount the price available to us. If the discount in not given default discount of 5% is applied |
| Task 2 | Factorial Utiity | The recursive function is used to calculate the factorial
| Task 3 | GST Calculator | Lambda function is used to calculate the Gst on the discounted price
| Task 4 | Applying GST using the list of prices | Using map function to apply previous lambda function to the list of prices
| Task 5 | Filter Expensive Products | Using filter function to filter the prices above and below 500
| Task 6 | Combined Utility Function | Using map function to apply 10 %v discount on the list of item and then filtering the items above 300 on the list.Lambda function has been used to calculate the price
| Task 7 | Menu using functions | Add the item's price to the list , getting average value of the list, finding the maximum value of the item in list is done using function. While loop has been use for user to easily get access to the menu and calculate the list as per user wish. Error handling has also be taken care of
